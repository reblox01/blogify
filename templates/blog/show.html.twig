{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mx-auto py-8">
        <h1 class="text-3xl font-bold mb-4">{{ post.title }}</h1>
        <p class="text-gray-600 mb-6">By {{ post.user.name ?? 'Unknown' }} — {{ post.createdAt ? post.createdAt|date('Y-m-d') : '' }}</p>
        <div class="prose">
            {{ post.description|raw }}
        </div>
    </div>
{% endblock %}

